<UserControl
    x:Class="IIoT.Edge.UI.Shared.Widgets.Footer.FooterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    Height="30"
    Background="Transparent">

    <Border
        Background="#18181B"
        BorderBrush="#27272A"
        BorderThickness="0,1,0,0">
        <Grid Margin="16,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!--  左侧：设备编号  -->
            <StackPanel
                Grid.Column="0"
                VerticalAlignment="Center"
                Orientation="Horizontal">
                <materialDesign:PackIcon
                    Width="13"
                    Height="13"
                    VerticalAlignment="Center"
                    Foreground="#52525B"
                    Kind="ServerOutline" />
                <TextBlock
                    Margin="6,0,0,0"
                    VerticalAlignment="Center"
                    FontSize="11"
                    Foreground="#52525B">
                    <Run Text="设备号：" />
                    <Run Foreground="#A1A1AA" Text="{Binding DeviceCode, Mode=OneWay}" />
                </TextBlock>
            </StackPanel>

            <!--  中间：运行时长  -->
            <StackPanel
                Grid.Column="2"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Orientation="Horizontal">
                <materialDesign:PackIcon
                    Width="13"
                    Height="13"
                    VerticalAlignment="Center"
                    Foreground="#52525B"
                    Kind="TimerOutline" />
                <TextBlock
                    Margin="6,0,0,0"
                    VerticalAlignment="Center"
                    FontSize="11"
                    Foreground="#52525B">
                    <Run Text="运行时长：" />
                    <Run Foreground="#A1A1AA" Text="{Binding UpTime, Mode=OneWay}" />
                </TextBlock>
            </StackPanel>

            <!--  右侧：实时时钟  -->
            <StackPanel
                Grid.Column="4"
                VerticalAlignment="Center"
                Orientation="Horizontal">
                <materialDesign:PackIcon
                    Width="13"
                    Height="13"
                    VerticalAlignment="Center"
                    Foreground="#52525B"
                    Kind="ClockOutline" />
                <TextBlock
                    Margin="6,0,0,0"
                    VerticalAlignment="Center"
                    FontSize="11"
                    Foreground="#A1A1AA"
                    Text="{Binding CurrentTime, Mode=OneWay}" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>