<Window x:Class="IIoT.Edge.Shell.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:headerVM="clr-namespace:IIoT.Edge.UI.Shared.Widgets.SystemHeader;assembly=IIoT.Edge.UI.Shared"
        Title="IIoT Edge Client" Height="768" Width="1024"
        WindowStyle="None" AllowsTransparency="True" WindowStartupLocation="CenterScreen"
        Background="#121212" Foreground="#EAEAEA">

    <Window.Resources>
        <DataTemplate DataType="{x:Type headerVM:HeaderWidget}">
            <headerVM:HeaderView />
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="45" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Grid.RowSpan="2" Grid.Column="0" Background="#18181B" BorderBrush="#27272A" BorderThickness="0,0,1,0">
            <TextBlock Text="SysMenu预留位" Foreground="#555" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Border>

        <ContentControl Grid.Row="0" Grid.Column="1" Content="{Binding HeaderViewModel}" />

        <Border Grid.Row="1" Grid.Column="1" Background="#1E1E1E" Margin="2" CornerRadius="4">
            <TextBlock Text="主工作区" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#555555" FontSize="20" />
        </Border>
    </Grid>
</Window>